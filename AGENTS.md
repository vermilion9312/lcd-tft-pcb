# AGENTS.md

이 문서는 이 저장소의 단일 PCB 프로젝트를 SKiDL로 설계하고, EasyEDA Pro/JLCPCB/LCSC 흐름으로 실제 제작 가능한 회로도를 준비할 때 따르는 작업 지침이다.

MCU, 모터, 센서, 전원 전압, 커넥터, 부품 번호는 사용자가 명시했을 때만 고정 조건으로 취급한다.

## 기본 작업 원칙

- 사용자의 요청 범위만 작게 수정한다.
- 프로젝트의 현재 회로 구조를 우선 이해하고, 요청과 직접 관련된 파일만 수정한다.
- 공용 코드나 테스트는 필요한 검증 보강이 있을 때만 건드린다.
- 사용자가 정한 부품, 전압, 전류, 기구 조건은 우선순위가 높다.
- 사용자가 정하지 않은 항목은 회로 목적, 전류/전압 정격, 조립 난이도, EasyEDA/JLCPCB 호환성, JLCPCB 실제 재고를 기준으로 보수적으로 선택한다.
- 전원 입력은 한 곳으로 한다. 같은 PCB 안에서 서보/모터 전원과 Blue Pill/MCU 로직 전원이 모두 필요하면, 별도 로직 5V 입력 커넥터를 추가하지 말고 보드의 단일 입력 전원에서 buck/LDO 등으로 필요한 전압을 만든다.
- 디버그 편의를 이유로 `LOGIC_5V_IN`, `SERVO_6V_IN`, `VBAT_IN` 같은 외부 전원 입력을 둘 이상 두지 않는다. 출력 커넥터, 하위 시트 포트, 테스트포인트는 전원 입력으로 세지 않지만, 이름과 주석에서 입력/출력 역할을 명확히 구분한다.
- 새로 선정하는 실장 부품은 원칙적으로 JLCPCB PCB Assembly에서 재고가 있는 부품만 선택한다.
- JLCPCB 재고가 없거나 조립 서비스 지원 여부를 확인하지 못한 부품은 구현 회로의 확정 소자로 넣지 않고, 필요하면 대체품을 다시 찾거나 `구현 제외`, `별도 검토`로 남긴다.
- MLCC를 새로 선정할 때는 정격, 패키지, 용량, DC bias, JLCPCB 재고 조건이 맞는 범위에서 우선적으로 `Brand:SAMSUNG(三星)` 부품을 사용한다.
- 위험도가 높은 회로는 단정적으로 완성했다고 말하지 않는다. 특히 220V AC, LiPo 배터리, 고전류 모터/서보, 고전력 buck, 충전 회로, 보호 회로는 주의점과 검증 필요성을 명확히 남긴다.

## SKiDL 작성 규칙

- 회로의 실행 진입점은 프로젝트 루트 또는 회로 폴더의 `main.py`에 둔다.
- 회로 구현은 객체지향 설계로 작성한다. `main.py`는 실행 진입점으로만 두고, 실제 회로 구현은 목적별 개별 파일에 둔다.
- 여러 회로 블록이 공유하는 실행 흐름은 `common/` 아래 공용 베이스 클래스로 만든다. 예: `common/circuit_base.py`의 `SkidlCircuit`.
- 전원 입력, buck, Blue Pill 소켓, 서보 출력, 보호 회로처럼 기능이 다른 회로는 블록별 파일로 나눈다. 예: `power_blocks.py`, `bluepill_blocks.py`, `servo_blocks.py`, `protection_blocks.py`, `circuit.py`.
- `circuit.py`는 프로젝트 회로 클래스를 조립하고, `main.py`에는 `Circuit().run("프로젝트명.net")`처럼 객체를 실행하는 코드만 남긴다. `main.py`에 부품/넷/연결 정의를 직접 길게 쓰지 않는다.
- 수동 파일 편집은 `apply_patch`를 사용한다.
- SKiDL 코드는 사용자가 EasyEDA Pro에서 회로도를 옮겨 그릴 수 있도록 읽기 쉬운 블록 단위로 작성한다.
- 부품 정의부는 주석 섹션으로 나눈다.
  - 예: `# === 전원 입력/보호 블록 ===`
  - 예: `# === Blue Pill 5V buck/소켓 블록 ===`
  - 예: `# === 서보 전원 분배 블록 ===`
- 기존 회로 개념을 재사용하지만 부품값이나 구조가 바뀌면 `재사용한 개념` 또는 `확장 블록`이라고 적어, 단순 복붙 부품인지 새로 설계한 부품인지 구분한다.
- 네트 이름은 목적이 드러나게 붙인다.
  - 예: `VBAT_IN`, `VBAT_PROTECTED`, `VLOGIC_5V`, `MOTOR_12V`, `J4PGND`, `GND_LOGIC`, `PWM_CH1`
- SKiDL에서 직접 변수로 선언하는 넷은 Python 변수명과 회로도 넷리스트 이름 문자열을 동일하게 맞춘다.
  - 예: `PWM_PIN = Net("PWM_PIN")`
  - 예외: 딕셔너리/리스트에서 동적으로 만드는 넷은 키 이름이나 생성 규칙이 회로도 넷 이름을 명확히 드러나게 작성한다.
- 커넥터 핀 순서는 주석에 명확히 적는다.
- 실제 핀 번호가 모듈/심볼마다 달라질 수 있으면 “EasyEDA Pro에서 실제 심볼과 대조”라고 주석을 남긴다.
- 구매 부품이 아닌 회로도 포트, 하위 시트, 테스트용 자리에는 임의 LCSC Part#를 붙이지 않는다.
- 극성이나 방향이 있는 부품은 `핀 정의:` 주석을 반드시 남긴다.
  - 다이오드/TVS/쇼트키: `Pin1=K(캐소드)`, `Pin2=A(애노드)`처럼 적고, 어느 핀이 전원/스위칭 노드/GND에 가는지 쓴다.
  - LED: `Pin1=K(캐소드)`, `Pin2=A(애노드)`처럼 적고, 애노드가 저항/전원 쪽인지 확인한다.
  - 전해 캐패시터: `Pin1=+`, `Pin2=-`를 적는다.
  - MOSFET: `G/D/S` 또는 `G/D/S/B`가 각각 어느 네트에 연결되는지 적는다.
  - 트랜지스터: `B/C/E` 또는 `G/D/S`가 각각 어느 네트에 연결되는지 적는다.
  - IC/레귤레이터: `VIN/GND/SW/FB/EN/BST`처럼 방향과 기능이 중요한 핀의 연결 네트를 적는다.
  - 커넥터: `Pin1=...`, `Pin2=...`처럼 실제 하네스 방향을 적는다.
  - 퓨즈, 0R, 테스트포인트처럼 무극성 부품도 `무극성` 또는 `Pin1=측정 노드`처럼 의도를 적는다.
  - EasyEDA Pro에서 사용하는 심볼/풋프린트의 핀 번호가 SKiDL/KiCad와 다를 수 있으므로 최종 회로도 작성 시 반드시 대조한다.

## 부품 주석 규칙

각 소자에는 가능한 한 다음 주석을 남긴다.

- 한국어 명칭
- 선택 이유
- 주요 사양
- EasyEDA 검색어
- LCSC Part#

예:

```python
# U1: 5V buck regulator
# 한국어 명칭: 5V 벅 레귤레이터 IC
# 선택 이유: 배터리 전압에서 로직용 5V를 만든다.
# 사양: 입력 4.5V~18V, 출력 5V, 2A, 패키지 TSOT-23-6.
# EasyEDA 검색어: AP63205WU-7
# LCSC Part#: C2071056 (Diodes AP63205WU-7, fixed 5V 2A buck regulator, TSOT-23-6)
```

- LCSC Part# 주석은 실제 부품의 제조사, 모델명, 패키지, 정격과 맞아야 한다.
- LCSC Part# 주석은 JLCPCB PCB Assembly 재고가 있는 부품을 기준으로 작성하고, 가능하면 `JLCPCB 재고: 확인됨`처럼 확인 상태를 함께 적는다.
- MLCC의 LCSC Part# 주석은 가능한 경우 Samsung 모델명과 `Brand:SAMSUNG(三星)` 여부를 확인해 적는다. Samsung MLCC가 재고/정격/패키지 조건에 맞지 않을 때만 다른 브랜드를 선택하고 이유를 남긴다.
- 사용자가 재고 부족이나 재고 불안정을 지적한 MLCC Part#는 삼성 부품이라도 확정 소자로 계속 쓰지 않는다. 테스트의 금지 코드 목록에 이유를 기록하고, 같은 용량/전압 이상/패키지 조건에서 JLCPCB PCB Assembly 재고가 충분한 대체품을 선정한다.
- 극성 부품은 LCSC 이미지와 데이터시트의 마킹도 확인한다. SMD 다이오드의 줄/띠 표시는 보통 캐소드 쪽이다.
- PCB에 실제로 실장/납땜할 수 있는 풋프린트, 핀 정의, 기구 체결, 정격을 확인하지 못한 부품은 구현 회로에 넣지 않는다.
  - 예: 40A 메인 경로용인지 확인 안 된 토글/락커 스위치, PCB 풋프린트가 불명확한 하네스 부품, 정격 부족 홀더.
  - 필요하면 `구현 제외`, `별도 검토`, `하네스/기구 영역`으로 주석만 남기고 SKiDL 부품으로 만들지 않는다.
- 블레이드/자동차 퓨즈처럼 교체형 퓨즈 본체와 홀더가 분리되는 부품은 반드시 암수와 Part#를 함께 병기한다.
  - 예: `퓨즈(수) C151095 ...`, `홀더(암) C19727305 ...`
  - 퓨즈 정격뿐 아니라 홀더(암)의 전류/전압/기구/하네스 정격도 회로 전류에 맞는지 별도로 적는다.
  - 재고가 충분하고 정격이 맞는 암수 짝을 찾지 못하면 교체형 퓨즈로 억지 선정하지 않고, 납땜형/보드실장 퓨즈처럼 `교체형 아님`, `홀더: 해당 없음`으로 명확히 적는다.
- `LCSC Part#: 미확정`은 작업 중 임시로만 허용한다. 사용자가 구현을 진행하라고 한 뒤에는 남기지 않는다.
- 실제 구매 부품이 아니면 `LCSC Part#: 해당 없음`이라고 쓰고 이유를 설명한다.
- `PLACEHOLDER`는 확정 구현에 남기지 않는다. 단, 의도적으로 하위 회로 포트임을 나타내는 경우에는 `PORT`, `SHEET_PORT`처럼 표현하고 주석으로 설명한다.

## LCSC Part# 검증 규칙

- 부품 번호는 반드시 실제 부품 종류, 패키지, 정격, 이미지와 맞춰 본다.
- 새 부품을 선정할 때는 LCSC Part# 존재만으로 확정하지 않고, JLCPCB PCB Assembly에서 현재 재고가 있는지 확인한다.
- JLCPCB 재고가 없는 부품은 확정 소자로 사용하지 않는다. 같은 기능/정격의 재고 있는 대체품을 우선 찾는다.
- 사용자가 Part# 오류를 지적하면 해당 코드와 이유를 테스트나 문서에 기록해 재사용을 막는다.
- 예: `C6142744`는 Samsung 22uF 10V X7R 1206 MLCC지만 재고가 불안정하다고 지적되어 확정 소자로 재사용하지 않는다. 대체 예시는 `C12891` Samsung CL31A226KAHNNNE 22uF 25V X5R 1206 MLCC처럼 재고가 충분한 부품이다.
- 새 LCSC 코드를 추가하면 가능하면 테스트에 다음을 함께 추가한다.
  - 허용된 LCSC 코드 목록
  - 해당 코드 주석에 반드시 포함되어야 하는 제조사/모델/정격/패키지 조각
  - JLCPCB PCB Assembly 재고 확인 문구
- Part#와 설명이 불일치할 때는 설명을 억지로 맞추지 말고 올바른 Part#를 다시 선정한다.
- JLCPCB 재고는 최신성이 있으므로, 새 부품을 확정하기 전에는 JLCPCB/LCSC 같은 실제 부품 페이지를 확인한다.

## 회로 안전과 정격

- 커넥터, 퓨즈, 스위치, 저항, 인덕터, MOSFET, 캐패시터는 전압/전류/온도/패키지 정격을 회로 목적에 맞게 고른다.
- 고전류 경로에는 작은 신호용 커넥터나 얇은 패턴을 쓰지 않는다.
- 고전류 전원 입력/출력/분배 커넥터는 원칙적으로 `KF301` 시리즈 단자대를 사용한다. 실제 전류, 피치, 핀 수, 하네스 굵기, JLCPCB 재고와 풋프린트가 맞는 세부 Part#를 확인해 확정한다.
- 저전류 신호/센서/로직 보조 전원 커넥터는 원칙적으로 `Molex 5267` 시리즈 커넥터를 사용한다. 실제 핀 수, 전류 정격, 하우징/터미널 암수 짝, JLCPCB 조립 가능 여부를 확인해 확정한다.
- 퓨즈는 보호하려는 배선, 커넥터, 부하 정격과 함께 선정한다.
- 메인 퓨즈와 분기 퓨즈의 역할을 구분한다.
  - 메인 퓨즈는 배터리/외부 전원 입력 직후에 두어 전체 입력 배선과 보드를 보호한다.
  - 분기 퓨즈는 실제로 전원이 여러 고전류 하네스/커넥터/부하로 갈라질 때, 각 분기 배선 정격에 맞춰 둔다.
  - 분기 구조가 없는 단일 입력/단일 출력 회로에는 같은 역할의 퓨즈를 반복해서 넣지 않는다.
  - 로직 buck 출력처럼 전류 제한이 이미 낮고 별도 분기 배선 보호 목적이 불명확한 경우에는 PTC를 관성적으로 추가하지 않는다.
- 스위치가 전체 전류를 직접 끊는지, 게이트/Enable 신호만 제어하는지 구분한다.
- 0R 저항이나 넷타이는 기준점 표시용이지 대전류 경로로 가정하지 않는다.
- LiPo 배터리 회로는 역극성, 단락, 과전류, 커넥터 오삽입, 방전 전류, 배선 굵기를 함께 고려한다.
- 220V AC는 이 PCB에서 직접 다루지 않는 것을 기본으로 한다. 필요하면 절연 SMPS 모듈, 퓨즈, 접지, 크리프/클리어런스, 인증품 사용을 별도 검토 항목으로 둔다.

## PCB 트랙 폭 주석 규칙

- SKiDL 파일의 회로 연결부 바로 위에는 `PCB 트랙 폭 가이드(mm)` 주석 블록을 남긴다.
- 가능하면 실제 회로 연결 코드 오른쪽에도 트랙 폭을 인라인 주석으로 남긴다.
  - 예: `J1["1"] += F1["1"]  # 0.80mm`
  - 예: `SW_NODE += L1["1"]  # 1.00mm, 짧게`
  - 예: `SERVO_6V += connector["1"]  # 6.00mm 또는 copper pour`
- 트랙 폭은 반드시 mm 단위로 적는다.
- 신호선, 로직 전원, 스위칭 노드, 고전류 전원, GND 리턴을 구분해서 적는다.
- 전류가 큰 경로는 단순 트랙 폭만 쓰지 말고 copper pour, via stitching, 2oz copper, 버스바, 굵은 배선 필요 여부를 함께 적는다.
- 아래 값은 시작점일 뿐이며, 실제 제작 전에는 동박 두께, 허용 온도 상승, 길이, via 개수, 커넥터 정격, 제조사 DRC를 기준으로 다시 계산한다.
  - 저속 신호/PWM/ADC/Enable/FB: `0.25mm` 이상
  - 일반 로직 전원/MCU 소켓 분기: `0.50mm` 이상
  - 1A급 전원: `0.80mm` 이상
  - 2A급 buck 입출력: `1.00mm` 이상
  - 단일 소형 모터/서보 분기: `2.00mm` 이상
  - 5A급 전원 경로: `4.00mm` 이상 또는 copper pour
  - 10A급 전원 경로: `6.00mm` 이상 또는 copper pour
  - 30A~40A급 전원 경로: `10.00mm~12.00mm` 이상 copper pour와 via stitching 권장. 1oz 단일 트랙만으로 처리한다고 가정하지 않는다.
- 스위칭 노드(`SW`)는 전류가 크더라도 면적을 과하게 키우지 않는다. 필요한 폭을 확보하되 짧게 배치하고 FB/ADC/클럭 신호와 거리를 둔다.
- GND는 전류 리턴 경로가 실제로 어디로 흐르는지 기준으로 폭을 정한다. 로직 GND pour와 고전류 PGND pour를 무조건 한 덩어리로 섞지 않는다.

## 그라운드와 전원 분리

- 큰 전류가 흐르는 전원 GND와 민감한 로직/아날로그 GND는 필요하면 네트 이름으로 구분한다.
  - 예: `PGND`, `PGND_MOTOR`, `PGND_SERVO`, `GND_LOGIC`, `AGND`
- 분리한 GND는 회로 목적에 따라 단일점 연결, 넷타이, 0R 저항, 페라이트 비드 등을 검토한다.
- 완전 절연이 필요한 경우에는 절연 DC-DC, 옵토커플러, 디지털 아이솔레이터 등 별도 절연 부품이 필요하다.
- PWM, UART, ADC 같은 신호는 반드시 기준 GND 경로를 함께 고려한다.

## 모듈/개발보드 사용 규칙

- 특정 개발보드나 모듈을 사용하라는 지시가 있으면 핀헤더/소켓으로 꽂을 수 있게 표현한다.
- 개발보드 전용 규칙은 프로젝트 전체 규칙으로 가정하지 않는다.
- 예를 들어 Blue Pill, Arduino Nano, ESP32 DevKit, Raspberry Pi Pico 등은 사용자가 명시한 경우에만 해당 소켓/핀헤더 규칙을 적용한다.
- 모듈 핀 번호는 EasyEDA Pro의 실제 심볼/풋프린트와 반드시 대조하라고 주석을 남긴다.
- 개발보드/모듈 핀아웃은 기억에 의존하지 않는다. 사용자가 자료를 주면 그 자료와 공식/신뢰 가능한 핀아웃 문서를 교차검증한다.
- 소켓의 양끝이나 대각선 핀을 임의로 전원/GND로 가정하지 않는다. 각 핀 번호와 신호명을 주석에 명시한다.
- Blue Pill STM32F103C8T6을 쓰는 경우에는 일반적인 Blue Pill 핀맵 기준으로 왼쪽 Header 1은 `1=VBAT`, `5=PA0`, `6=PA1`, `7=PA2`, `18=3.3V`, `19=GND`, `20=GND`이고, 오른쪽 Header 2는 `1=3.3V`, `2=GND`, `3=5V`, `4=PB9` 순서임을 먼저 확인한다. 5V 입력은 오른쪽 Header 2 Pin3에 연결한다.

## 테스트와 검증

- 수정 후 가능한 검증을 실행한다.
  - `python -m pytest tests -q`
  - 수정한 `main.py`의 `python -m py_compile`
- 범위가 넓으면 관련 Python 회로 파일 전체를 `py_compile`한다.
- `py_compile` 후 생성된 `__pycache__`는 정리한다.
- `rg "미확정|PLACEHOLDER|C50975|C720477|C6142744"`처럼 문제 문구나 금지 코드를 검색한다.
- 검증 실패 시 테스트만 완화하지 말고, 회로/주석/테스트 중 무엇이 잘못됐는지 먼저 확인한다.

## 응답 방식

- 한국어로 답한다.
- 작업 완료 후에는 바꾼 파일, 확정한 부품, 통과한 검증을 간단히 정리한다.
- 사용자가 EasyEDA Pro에서 옮겨 그릴 수 있도록 실용적인 주석과 Part#를 우선한다.
- 위험하거나 아직 검증되지 않은 회로는 남은 리스크를 짧게 분명히 말한다.
